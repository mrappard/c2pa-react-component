import { CRIcon } from '../../../../icons/CRIcon'
import { LevelProps } from '../../types'

export function C2paManifestL1({ className }: LevelProps) {
  return (
    <div className={className} style={{ display: 'inline-block' }}>
      <CRIcon size={28} />
    </div>
  )
}
